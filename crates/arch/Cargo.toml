[package]
name = "ignition-arch"
version = "0.0.0"
edition = "2024"
description = "Architecture support (boot regs, FDT, sysreg tables) for the ignition VMM"
license = "Apache-2.0"

[lib]
name = "ignition_arch"
path = "src/lib.rs"

[dependencies]
vm-fdt = "0.3"

[dev-dependencies]
fdt = "0.1"
