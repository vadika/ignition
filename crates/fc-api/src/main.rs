// Skeleton: later tasks (api.rs / vm.rs) consume the model types.
#![allow(dead_code)]
mod model;
mod config;

fn main() {
    eprintln!("ignition-fc-api: not yet wired (see api.rs/vm.rs tasks)");
    std::process::exit(1);
}
